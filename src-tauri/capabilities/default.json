{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "FollowCleaner V0.1 默认权限：核心 + 文件对话框（用户显式选择导入/导出路径）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
